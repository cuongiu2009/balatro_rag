# Progress Update (2026-04-05)

## Resolved Issues:

1.  **`init.lua` not printing to `ai_debug.txt`:**
    *   **Problem:** Lua `print` statements were not appearing in `ai_debug.txt`.
    *   **Resolution:**
        *   Identified that `E:\BalatroTest\balatro_mod\Balatro` is the directory dragged into `love.exe`.
        *   Confirmed `src/lua_game_hooks/init.lua` contains the correct `print` redirection logic.
        *   Corrected `log_file_path` in `src/lua_game_hooks/init.lua` from `love.filesystem.getSaveDirectory() .. "/ai_debug.txt"` to `./ai_debug.txt` (as per user's instruction).
        *   Switched from `love.filesystem.newFile` to `io.open` for direct file system access, resolving silent failures.
        *   **Status:** Resolved. `ai_debug.txt` is now being created and populated correctly in `E:\BalatroTest\balatro_mod\Balatro\ai_debug.txt`.

2.  **TCP communication issue ("Failed to receive response from Python TCP server: closed"):**
    *   **Problem:** Lua client connected and sent data to the Python TCP server, but the connection was closed before a response could be received.
    *   **Resolution:**
        *   Modified `src/python_orchestrator/communication/socket_server.py` to prefix its JSON responses with a 4-byte little-endian unsigned integer representing the length of the JSON data.
        *   Modified `src/lua_game_hooks/socket_client.lua` to expect and read this 4-byte length prefix before attempting to receive the full TCP response data.
        *   **Status:** Resolved. Subsequent TCP communications show successful response reception and JSON parsing.

3.  **HTTP response parsing issue ("HTTP request returned non-table 'res'. Type: number Value: 1"):**
    *   **Problem:** Lua client's `socket.http` call to FastAPI for AI suggestions (`http://127.0.0.1:8000/suggestion`) resulted in `res` being a `number` (`1`) instead of a `table` containing the response body, even when FastAPI returned `200 OK`. This prevented successful JSON parsing of the AI suggestion.
    *   **Resolution:** Modified `src/lua_game_hooks/socket_client.lua` to correctly use `ltn12.sink.table()` to collect response chunks into a table, and then `table.concat()` to reconstruct the full response body. This correctly extracted and allowed parsing of the JSON response from FastAPI.
    *   **Status:** Resolved. HTTP communication now successfully receives and parses AI suggestions.

## Additional Progress (2026-04-05):

1.  **Git Repository Setup:**
    *   Assisted in correctly configuring the Git repository.
    *   Managed `.gitignore` to exclude specified directories and files (`Balatro/`, `.specify/`, `.gemini/`, `GEMINI.md`).
    *   Resolved issues with remote origin configuration and branch merging.
2.  **Python Environment Setup:**
    *   Ensured Python virtual environment is set up and activated.
    *   Installed necessary dependencies, including `psutil`.
3.  **Testing:**
    *   Attempted to run Python tests, encountered and resolved `ModuleNotFoundError` by setting `PYTHONPATH`.
    *   Resolved `IndentationError` in `test_ai_suggestion_pipeline.py`.
    *   Encountered `ImportError` in `test_ai_suggestion_pipeline.py` due to refactored `socket_server.py`.
    *   **Status:** Test execution task cancelled as per user's explicit instruction.
4.  **Refined Error Handling and Logging (T026):**
    *   Reviewed existing Lua and Python components for error handling and logging. The current implementation largely aligns with the `plan.md` strategy (Lua errors to `ai_debug.txt`, Python errors logged and simplified messages sent to Lua).
    *   **Status:** Completed.
5.  **Defined Performance Monitoring Methodology (T027):**
    *   Implemented `tests/python/stress/performance_monitor.py`. This script defines a methodology for monitoring FPS and input lag, outlining how data would be collected and analyzed. It includes a simulation function for demonstration.
    *   **Status:** Completed.
6.  **Defined Stress Testing for Resource Usage (T028):**
    *   Implemented `tests/python/stress/test_resource_usage.py`. This script outlines a methodology for conducting stress tests to monitor CPU and RAM usage of the Python orchestrator. It uses `psutil` and includes a simulation function, along with a placeholder for VRAM monitoring considerations.
    *   **Status:** Completed.
7.  **Reviewed Code for Performance Optimization (T029):**
    *   Provided a comprehensive review of potential optimization areas for both Lua and Python components. This included suggestions for `data_extractor.lua`, `socket_client.lua`, FastAPI server, RAG processor, LM Studio client, and cross-component strategies like data serialization and logging verbosity.
    *   **Status:** Completed.

## Current Challenges & Debugging (2026-04-05):

1.  **Problem:** AI suggestion consistently returning the same generic result (e.g., "Play CS card"), despite initial HTTP/TCP communication fixes. This indicates the AI is not receiving dynamic game state for analysis.
2.  **Investigation & Findings (AI Suggestion Quality):**
    *   Confirmed `G.hand.cards`, `G.jokers.cards`, and `G.consumeables.cards` are the correct paths for accessing card/joker data.
    *   Confirmed that these data structures are populated during active gameplay.
    *   Identified that `serialize_cards` and `serialize_jokers` functions in `data_extractor.lua` are currently failing to extract specific `id` and `name` attributes from card/joker objects, leading to "Unknown Card" and "UnknownID" in the JSON payload sent to Python.
    *   Discovered that the `print_table_recursive` function's `max_depth` (initially 3, then increased to 7 for debugging) was insufficient to fully expose the nested structure of card/joker objects within the full `_G.G` dump.
    *   Added debug prints directly within `serialize_cards` and `serialize_jokers` to inspect the structure of individual card/joker objects. (These have now been removed as per user instruction).
3.  **Resolved Problem:** Python `TypeError: sequence item 0: expected str instance, dict found` when formulating prompt for AI.
    *   **Resolution:** Modified `src/python_orchestrator/rag_model/rag_processor.py` to correctly convert card dictionaries into descriptive strings (e.g., "King of Hearts") before joining them for the query text.
    *   **Status:** Resolved. The Python server can now successfully send prompts to LM Studio and receive suggestions.
4.  **Persistent Problem (Impact on AI Suggestion Quality):** Despite the Python error fix, AI suggestions remain generic and sometimes illogical (e.g., suggesting a "flush" with two Kings when a pair is obvious).
    *   **Root Cause:** This is a direct consequence of the "Unknown Card/ID" issue in the Lua `data_extractor.lua`. The LM Studio model receives `player_hand` and `active_jokers` as lists of dictionaries where each card/joker object has `name: "Unknown Card"` and `id: "UnknownID"`. Without proper unique identifiers and names, the AI cannot reason about specific card instances, their enhancements, or unique joker abilities to provide nuanced strategic advice.
5.  **User Decision:** User has explicitly stated that extracting the correct `id` and `name` for cards and jokers is currently too time-consuming and can be deprioritized. For now, the system should proceed with extracting `rank` and `suit` for cards, and the basic `name`, `value`, and `effect` for jokers, even if `id` and detailed `name` remain "Unknown". This is deemed acceptable for a preliminary functional product. However, it's now clear that addressing this is crucial for meaningful AI suggestions.
6.  **Python Script for Log Parsing:** Developed and refined `src/python_orchestrator/scripts/parse_ai_debug_log.py` to efficiently extract the last full `_G.G` table dump from `ai_debug.txt`.

## Next Steps:
*   **Temporarily accept current card/joker serialization:** Proceed with the current level of card/joker detail (rank, suit for cards; basic name, value, effect for jokers). The deeper analysis for `id` and specific `name` attributes will be a future task, as it requires more in-depth Lua reverse engineering.
*   **Focus on core AI pipeline:** Ensure the overall flow from game state extraction, through Python processing, to LM Studio suggestion, and back to Lua display is stable and functional with the current data.
